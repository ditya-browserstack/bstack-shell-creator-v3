# generated handoff artifacts (regenerable via handoff-to-sync.mjs)
design/products/*/app-shell/shots/
design/products/*/app-shell/cards/

# macOS + temp gallery
.DS_Store
**/.DS_Store
design/products/*/app-shell/cards-gallery.html

# RAW working captures hold real data — never commit (share/ is the committed build).
# The clean _reference-lca example is exempt so the skill ships a working demo.
design/products/*/app-shell/multiscreen-shell.html
!design/products/_reference-lca/app-shell/multiscreen-shell.html

# self-check artifacts (screenshots + prod refs — regenerable, may hold real data)
design/products/*/app-shell/self-check/
.last-update-check

# Internal reference capture (BrowserStack product UI) — kept LOCAL/bundle only, never public
design/products/_reference-lca/
